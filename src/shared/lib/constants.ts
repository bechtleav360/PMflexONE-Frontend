/**
 * Marker appended to required-field labels to signal that the field must be filled.
 * Always wrap in `<span aria-hidden="true">` so screen readers do not announce it.
 */
export const REQUIRED_MARKER = '*'

/**
 * Decorative breadcrumb separator character.
 * Always wrap in `<span aria-hidden="true">` so screen readers skip it.
 */
export const CRUMB_SEP = '/'

/**
 * En-dash used as a decorative range separator (e.g. "from – to").
 * Always wrap in `<span aria-hidden="true">` so screen readers skip it.
 */
export const EN_DASH = '–'
